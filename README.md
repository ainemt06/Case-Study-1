## HI DANIEL

When you open this for the first time, run this:

`python -m venv venv`

`source venv/bin/activate`

`pip install -r requirements.txt`

I made a venv so it wouldn't explode when we updated it :)